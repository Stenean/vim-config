kchmck/vim-coffee-script:
    npm install -g coffee-script
    npm install -g coffeelint

Syntastic:
    pip install flake8 frosted
    #frozen
    git clone https://github.com/htacg/tidy-html5

Isort:
    pip install isort

make:
    pip install nose django_nose git+git://github.com/lambdalisue/nose-machineout.git#egg=nose_machineout

tern_for_vim:
    npm install tern_for_vim

tmux:
    wa

system:
    sudo -H pip install httpie powerline-status
    sudo apt-get install xombrero
