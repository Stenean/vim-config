kchmck/vim-coffee-script:
    npm install -g coffee-script
    npm install -g coffeelint

Syntastic:
    pip install flake8 frosted
    #frozen
    git clone https://github.com/htacg/tidy-html5

Isort:
    pip install isort

make:
    pip install nose
    pip install git+git://github.com/lambdalisue/nose-machineout.git#egg=nose_machineout

tern_for_vim:
    npm install in tern_for_vim
